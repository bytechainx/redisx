# redisx 上下文

本文件定义 `redisx` 与其使用方共享的核心词汇。它只记录领域含义与能力边界，
不记录具体实现、API 签名、存储或部署决定。

## 角色与边界

**适配器**：把 Redis 的 wire 协议（连接拓扑、命令、Streams、Pub/Sub）收敛成稳定 Rust API 的库；
它不承载缓存语义、不做落库或编排，也不实现服务端。
_Avoid_: 缓存中间件（本仓库不提供旁路缓存、刷新策略或失效编排）

**配置**：构造期一次性校验、之后只读的声明式参数集合；校验失败即拒绝构造，
不提供「先构造、后补验」的路径。
_Avoid_: 运行时设置（本仓库没有可变更的运行时配置面）

**拓扑模式**：`RedisMode` 对连接建立方式的分类（`Standalone` / `Cluster` / `Sentinel`）；
它决定发现与重连路径，不改变命令语义。
_Avoid_: 部署形态（拓扑是协议侧的建连方式，不描述机房或副本布局）

**连接池**：以 `max_in_flight` 许可与 `acquire_timeout` 约束并发与等待的复用闸门；
它是可 `Clone` 的共享句柄，克隆不复制连接。
_Avoid_: 全局单例（池的共享范围由调用方持有句柄决定，不是进程级隐含状态）

## 命令与会话

**命令客户端**：在既有池之上承载命令发送、重试准入与脚本 / 事务编排的句柄（`RedisClient`）；
它不自己持有物理连接。
_Avoid_: 连接（客户端用法不涉及建连与重连，那是池的职责）

**逻辑库**：Redis 实例内以数字区分的键空间；它与拓扑正交，且 `Cluster` 只支持逻辑库 0。
_Avoid_: 数据库（Redis 的逻辑库不提供表与 schema 隔离）

**事务批次**：以 `MULTI` / `EXEC` 打包、按序提交的一组命令（`TxCmd`）；它是单实例内的
原子执行单元，不是跨 shard 事务。
_Avoid_: 分布式事务（本仓库不提供跨分片或跨实例的原子性）

**流条目**：Streams 返回的单条记录（`StreamEntry`），带自己的槽位标识；它只描述数据，
不自带消费位点或确认语义。
_Avoid_: 消息（消息强调投递语义，本仓库不提供确认与重投）

## 副作用与重试

**重试安全分类**：`RedisRetrySafety` 对命令副作用的判定，取值 `ReadOnly` / `Idempotent` /
`AmbiguousWrite` / `NeverAutomatic`；它是「能否自动重试」的唯一判据。
_Avoid_: 错误可重试性（那是「这次失败是否瞬态」，与「重放该命令是否安全」是两件事）

**只读 / 幂等命令**：重复施加不改变最终状态、因而可进入自动重试环的命令类别。
_Avoid_: 查询类命令（分类依据是副作用而非是否返回值）

**结果不明写**：已发出但结果未知的写命令（超时或连接中断），重放可能造成重复副作用，
因此**永远**只执行一次。
_Avoid_: 失败命令（失败是已判定的事实，结果不明恰恰是尚未判定）

**重试预算**：以总 deadline 约束的重试上限，退避为指数增长并叠加抖动；超出预算即放弃。
_Avoid_: 无限重试 / 固定间隔（前者无界，后者在抖动场景下会同步重试风暴）

## 背压与可靠性

**有界背压**：以许可上限 + 等待超时限制在途命令数的机制；请求在超时后失败而非排队到无界。
_Avoid_: 队列缓冲（本仓库不引入无界队列，积压以显式超时表达）

**可达性探测**：`ping` / `health_check` 对服务的显式探测，与构造解耦；构造成功不代表服务可达。
_Avoid_: 建连（`connect` 只做校验与构造，不发命令）

**凭据脱敏**：密码在 `Debug` 与端点展示中一律输出为 `***`，且只能经环境变量或 builder 注入。
_Avoid_: 隐藏字段（脱敏针对的是输出与日志，不是字段可见性本身）

**可重试错误**：`RedisError::is_retryable()` 判定的瞬时故障类别，与业务拒绝相区分。
_Avoid_: 网络错误（网络只是成因之一，不能表达「重试是否安全」这一判据）
